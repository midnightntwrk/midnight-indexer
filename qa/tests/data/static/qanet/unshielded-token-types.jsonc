// Custom (non-NIGHT) unshielded token types the qanet chain has been provisioned with,
// used by tests/e2e/unshielded-transactions.test.ts. Each entry must be a token type the
// wallet behind FUNDING_SEED_QANET can spend, since that suite transfers it from there.
//
// Snapshot date: 2026-08-10 (verified against indexer.qanet.midnight.network and
// indexer-blue.qanet.midnight.network, chain tip 2157122).
{
  // Contract-minted token, created at block 14817. The funding wallet holds it in a single
  // unspent UTXO of 1000000000, so the suite's 1-unit transfers can run for a very long
  // time before it is exhausted. Two other custom token types sit in the same wallet
  // (4254045154a50886812969a17eee2840934bc12b26a968fe3f7e1929c03dfe01, also 1000000000,
  // and f015e1fc6210eacf15006450cd70ee55486c86a5ce7508f571cb88810a831d78, only 1100) if
  // this one is ever spent down.
  "custom": "27b7cfdff3d67408890de83ab711d0066cd89b436b15a5723bc21aed82a74130"
}
